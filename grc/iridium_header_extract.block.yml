# auto-generated by grc.converter

id: iridium_header_extract
label: header_extract
category: '[Iridium]'

parameters:
-   id: correlation_sample_rate
    label: Correlation Sample Rate
    dtype: int
-   id: output_samples
    label: Output Samples
    dtype: int
-   id: search_depth
    label: Search Depth
    dtype: int
-   id: hard_max_queue_len
    label: Hard Max Queue Length
    dtype: int
-   id: input_taps
    label: Input Taps
    dtype: real_vector
-   id: start_finder_taps
    label: Start Finder Taps
    dtype: real_vector
-   id: handle_multiple_frames_per_burst
    label: Handle Multiple Frames Per Burst
    dtype: bool
    default: 'False'

inputs:
-   domain: message
    id: cpdus

outputs:
-   domain: message
    id: cpdus
-   domain: message
    id: burst_handled

templates:
    imports: import iridium
    make: iridium.burst_downmix(${correlation_sample_rate}, ${output_samples}, ${search_depth}, ${hard_max_queue_len},
        ${input_taps}, ${start_finder_taps}, ${handle_multiple_frames_per_burst})

file_format: 1
